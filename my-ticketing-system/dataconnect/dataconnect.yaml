specVersion: "v1"
serviceId: "my-ticketing-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-ticketing-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
